name: Apply El Topo v0.9.1
on:
  push:
    paths:
      - 'eltopo/tools/apply_v091_superadmin_finish.py'
      - '.github/workflows/apply-v091.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply v0.9.1 finish
        run: python eltopo/tools/apply_v091_superadmin_finish.py
      - name: Validate JavaScript
        run: |
          node --check eltopo/social-game.js
          node --check eltopo/mixed-avatar-sync.js
          node --check eltopo/superadmin/admin.js
      - name: Commit v0.9.1
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add eltopo/social-game.js eltopo/index.html eltopo/mixed-avatar-sync.js eltopo/superadmin/index.html eltopo/superadmin/admin.js
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Finish El Topo superadmin tools v0.9.1'
            git push
          fi
