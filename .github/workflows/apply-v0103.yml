name: Apply El Topo v0.10.3
on:
  push:
    paths:
      - 'eltopo/tools/apply_v0103_incognito_chat_order.py'
      - '.github/workflows/apply-v0103.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Incognito chat ordering
        run: python eltopo/tools/apply_v0103_incognito_chat_order.py
      - name: Validate JavaScript
        run: |
          node --check eltopo/social-game.js
          node --check eltopo/mixed-avatar-sync.js
          node --check eltopo/superadmin/admin.js
      - name: Commit v0.10.3
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add eltopo/social-game.js eltopo/index.html eltopo/mixed-avatar-sync.js eltopo/superadmin/admin.js eltopo/superadmin/index.html
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Synchronize Incognito chat ordering through host'
            git push
          fi
